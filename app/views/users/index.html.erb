<%= link_to "投稿画面へ","/users/new" %>
<% @users.each do |user| %>
  <p>
    <%= user.name %>
    <%= user.age %>
    <%= link_to"編集", "/users/:id/edit", method: :get %>
    <%= link_to "削除","/users/#{user.id}", method: :delete, data: {confirm: "削除する？"} %>
  </p>

<% end %>
